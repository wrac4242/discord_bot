{
  "name": "discord_bot",
  "version": "1.0.0",
  "description": "discord bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd build && node ./index.js",
    "build": "npm install && tsc",
    "reinstall": "git pull && npm run build"
  },
  "author": "wrac4242",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^16.0.1",
    "discord.js": "^13.1.0",
    "dotenv": "^10.0.0",
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": "^16.0.0",
    "npm": "^7.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wrac4242/discord_bot.git"
  }
}
