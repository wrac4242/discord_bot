{
  "name": "discord_bot",
  "version": "1.0.0",
  "description": "discord bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install typescript && tsc && cd build && node ./index.js"
  },
  "author": "wrac4242",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^15.0.1",
    "discord.js": "^12.5.3",
    "dotenv": "^8.2.0",
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": "^16.0.0",
    "npm": "^7.10.0"
  }
}
