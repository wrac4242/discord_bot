{
  "name": "discord_bot",
  "version": "1.0.0",
  "description": "discord bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd build && node ./index.js",
    "watch-ts": "tsc -w",
    "postinstall": "tsc",
  },
  "author": "wrac4242",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.3",
    "dotenv": "^8.2.0",
    "@types/node": "^15.0.1"
  },
  "engines": {
      "node": ">=0.0.0",
      "npm": ">=0.0.0"
    }
}
